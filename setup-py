# setup.py
from setuptools import setup, find_packages

setup(
    name="changex-neurix",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        "Flask>=2.3.3",
        "gunicorn>=21.2.0",
        "python-dotenv>=1.0.0",
        # Add more as needed
    ],
    python_requires=">=3.8",
)
